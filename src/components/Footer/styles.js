import styled from 'styled-components';

export const Footer = styled.div`
  background: var(--coal);
  color: var(--white);
  margin-bottom: 0;
  min-height: 33vh;
  padding: 5rem 2.5rem 1.25rem;
`;
